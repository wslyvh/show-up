{
  "name": "subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:scroll": "graph build --network scroll-sepolia",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ show-up-protocol",
    "deploy:scroll": "graph deploy --node https://api.studio.thegraph.com/deploy/ show-up  --network scroll-sepolia",
    "create-local": "graph create --node http://localhost:8020/ show-up-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ show-up-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 show-up-protocol",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
